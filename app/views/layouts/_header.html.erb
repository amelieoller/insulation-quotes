<nav class="navbar navbar-default">
   <div class="container-fluid">
      <!-- Mobile hamburger menu -->
      <div class="navbar-header">
         <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
         </button>
         <a href="<%= root_path %>" class="navbar-brand">Insulation Quotes</a>
      </div>

      <!-- Main navigation -->
      <div class="collapse navbar-collapse" id="navbar-collapse-1">
         <ul class="nav navbar-nav">

            <% if user_signed_in? %>
               <li class="dropdown">
               <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Quotes<span class="caret"></span></a>
                  <ul class="dropdown-menu">
                     <li><a href="<%= quotes_path %>">All Quotes</a></li>                        
                     <li><a href="<%= new_quote_path %>">Create New Quote</a></li>
                  </ul>
               </li>

               <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Insulation Types<span class="caret"></span></a>
                  <ul class="dropdown-menu">
                     <li><a href="<%= insulation_types_path %>">All Insulation Types</a></li>
                     <li><a href="<%= new_insulation_type_path %>">Create New Insulation Type</a></li>
                  </ul>
               </li>

               <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>               

            <% else %>
               <li><a href="<%= new_user_registration_path %>">Sign Up</a></li>
               <li><a href="<%= new_user_session_path %>">Login</a></li>
            <% end %>

         </ul>

         <% if user_signed_in? %>
            <p class="navbar-text navbar-right">Signed in as <a href="<%= edit_user_registration_path %>"><%= current_user.email %></a></p>
         <% end %>

      </div>
   </div>
</nav>